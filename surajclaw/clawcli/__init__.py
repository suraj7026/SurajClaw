"""SurajClaw command-line interface.

This package is a thin WebSocket + REST client for the Django backend in
`surajclaw/`. The agent loop, slash commands, and tool dispatch all run
server-side; this package only renders the streamed frames.
"""
from __future__ import annotations

__version__ = "0.1.0"
