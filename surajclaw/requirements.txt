# --- Django core -----------------------------------------------------------
Django>=5.0,<6.0
djangorestframework>=3.15
django-celery-beat>=2.6
django-celery-results>=2.5
django-cors-headers>=4.3
django-filter>=24.2

# --- Async / WebSocket -----------------------------------------------------
channels>=4.0
daphne>=4.1

# --- Background tasks ------------------------------------------------------
celery>=5.4
sqlalchemy>=2.0

# --- Database --------------------------------------------------------------
psycopg[binary]>=3.1
pgvector>=0.3

# --- LLM & Agents ----------------------------------------------------------
langgraph>=0.2
langchain>=0.2
langchain-community>=0.2
langchain-google-genai>=2.0

# --- Google Workspace ------------------------------------------------------
google-api-python-client>=2.120
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# --- HTTP / parsing --------------------------------------------------------
httpx>=0.27
beautifulsoup4>=4.12
feedparser>=6.0
docker>=7.0

# --- Git operations --------------------------------------------------------
gitpython>=3.1

# --- Config / env ----------------------------------------------------------
python-dotenv>=1.0

# --- Cron / scheduling -----------------------------------------------------
# Used by scheduler.cron_runner to compute next-fire times for cron-style
# CronJob schedule values.
croniter>=2.0

# --- PDF / Markdown --------------------------------------------------------
markdown>=3.6
